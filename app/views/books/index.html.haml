%h1 Listing books

%table
  %tr
    %th Title
    %th Tag
    %th
    %th
    %th
  - books.each do |book|
    %tr
      %td= book.title
      %td= book.tag
      %td= link_to 'Show', book_chapters_path(book)
      %td= link_to 'Edit', edit_book_path(book)
      %td= link_to 'Destroy', book, :confirm => 'Are you sure?', :method => :delete
%br
= link_to 'New Book', new_book_path
